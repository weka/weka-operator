# EKS Dev Cluster

## Overview

This is a cluster that runs Weka in Kubernetes on AWS EKS.
It provides separate node pools for frontend and backend workloads.
Weka runs _only_ in Kubernetes managed containers.
It is not installed directly on the EC2 instances.
