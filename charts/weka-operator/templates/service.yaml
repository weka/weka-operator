apiVersion: v1
kind: Service
metadata:
  name: {{ include "weka-operator.fullname" . }}
  labels:
    {{- include "weka-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weka-operator.selectorLabels" . | nindent 4 }}
