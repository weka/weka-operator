# More info: https://docs.docker.com/engine/reference/builder/#dockerignore-file
# Ignore build and test binaries.
bin/
testbin/
dist/
.git
examples
node_modules
terraform
packer
ansible
.github
doc
# Holds git credentials on release builds; excluded so `COPY ./ /workspace` cannot
# bake them into a layer. image.Dockerfile mounts them as build secrets instead.
dockerfile_files
